name: Makefile CI

on:
  push:
    branches: [ "ci" ]
#  pull_request:
#    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Build
      run: make build

    - name: Run check
      run: make lint

    - name: Run test
      run: make test
